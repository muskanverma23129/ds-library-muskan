## Build Log – Session 1

**Date:** July 6, 2026

**Duration:** 1 Hour 45 Minutes

### Goal

Design and implement the foundational operations of the LinkedList data structure.

### Problem Encountered

No major implementation issues were encountered. The primary objective was to establish the basic structure of the LinkedList and verify that node insertion behaved correctly.

### What I Tried

Before generalizing the implementation, I first created the LinkedList using the `int` data type. This allowed me to focus on pointer manipulation without introducing template-related complexity.

During this session, I implemented:

- Constructor
- Destructor
- `insertFront()`
- `insertBack()`
- `insert(index)`

After implementing each function, I executed small test programs to verify that nodes were linked correctly and that the `head`, `tail`, and `size` members were updated appropriately.

### Outcome

The fundamental insertion operations were completed successfully. The LinkedList correctly stored integer values and maintained proper pointer connections after each insertion.